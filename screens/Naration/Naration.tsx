import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const Naration = (props: Props) => {
  return (
    <View>
      <Text>Naration</Text>
    </View>
  )
}

export default Naration

const styles = StyleSheet.create({})